```java
1、简单的springboot个人博客（单机版）<br>
2、使用mybatis做持久层，MySql作为数据库，Springboot自动配置
3、里面嵌套了ES作为博客的搜索查询，WebSocket即时通信，当然也在部分用到了redis作为一个缓存
4、在处理消息或者说请求时，也使用了RBMQ作为消息队列，来处理消息
5、前端用了freemarker模板引擎，layui框架搭建

```

